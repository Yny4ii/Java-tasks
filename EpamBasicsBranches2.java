public class EpamBasicsBranches2 {
    public static void main(String[] args) {
        int a = 3;
        int b = 12;
        int c = 0;
        int d = -1;

        int resuls = Math.max(Math.min(a,b),Math.min(c,d));
        System.out.println("Максимальное значение {min(a, b), min(c, d)} -  " + resuls);
    }
}
/*Basics of software code development, Ветвления задача 2
Найти максимальное значение {min(a, b), min(c, d)} */

